// Create a function called greetUser
// It takes a name as input
function greetUser(name) {
  // Use template literals (backticks) to build the message
  return `Welcome to TechCrush, ${name}! You will excel in the Backend Development, ${name}.`;
  
}

// Test the function with my name
console.log(greetUser("Emediong"));