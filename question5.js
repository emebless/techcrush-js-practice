// Original array
const numbers = [3, 6, 9, 12, 15];

// Use map() to create a NEW array with doubled values
const doubledNumbers = numbers.map(num => num * 2);

// Log the new array
console.log(doubledNumbers);

//Show that the original array is unchanged
console.log(numbers); 