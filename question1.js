// This is my name (it will not change, so we use const)
const myName = "Emediong";

// This is my age (it might change as I grow, so we use let)
let myAge = 10;

// This tells if I am a student
const isStudent = true;

// Now we print (show) each value on the screen (console)
console.log(myName);     
console.log(myAge);      
console.log(isStudent); 